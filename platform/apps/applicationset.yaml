apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: services
  namespace: argocd
  annotations:
    # After the platform's APIs, which the services' requests need.
    argocd.argoproj.io/sync-wave: "2"
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        # A matrix takes exactly two generators, so the repositories share the stages' list.
        generators:
          - list:
              elements:
                - repo: https://github.com/hvpaiva/back-hello.git
                  stage: staging
                  branch: staging
                - repo: https://github.com/hvpaiva/back-hello.git
                  stage: production
                  branch: main
          - git:
              repoURL: '{{ .repo }}'
              revision: '{{ .branch }}'
              files:
                - path: charts/*/values.yaml
  template:
    metadata:
      name: '{{ .application.name }}-{{ .stage }}'
      labels:
        team: '{{ .application.team }}'
        stage: '{{ .stage }}'
    spec:
      project: apps
      sources:
        - repoURL: https://github.com/hvpaiva/back.git
          targetRevision: main
          path: charts/app
          helm:
            releaseName: '{{ .application.name }}'
            ignoreMissingValueFiles: true
            valueFiles:
              - '$values/{{ .path.path }}/values.yaml'
              - '$values/{{ .path.path }}/values-{{ .stage }}.yaml'
            parameters:
              - name: platform.stage
                value: '{{ .stage }}'
        - repoURL: '{{ .repo }}'
          targetRevision: '{{ .branch }}'
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{ .application.name }}-{{ .stage }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
