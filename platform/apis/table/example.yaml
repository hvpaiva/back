# A request, to render the Composition against (`just render table`) or to apply as it is.
apiVersion: back.lab/v1alpha1
kind: Table
metadata:
  name: visits
  namespace: hello-staging
spec:
  partitionKey:
    name: page
  sortKey:
    name: seenAt
    type: number
  timeToLiveAttribute: expiresAt
