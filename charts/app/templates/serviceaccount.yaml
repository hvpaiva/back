apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
# The service doesn't call the Kubernetes API, so it gets no token.
automountServiceAccountToken: false
