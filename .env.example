# Optional. Copy to .env (git-ignored).

# The GitHub App the lab reports deployments with, and pushes promotions as.
# Contents needs read and write; the client id is on the App's own settings page.
GITHUB_APP_ID=
GITHUB_APP_CLIENT_ID=
GITHUB_APP_INSTALLATION_ID=
GITHUB_APP_PRIVATE_KEY_FILE=~/Downloads/your-app.2026-01-01.private-key.pem
